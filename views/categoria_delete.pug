extends layout

block content
  h1= title

  if error
    p.text-danger= error

  if posts.length
    p No se puede eliminar la categoría "#{categoria.name}" porque tiene posts asociados. Primero elimine los posts y luego la Categoria.
    ul
      each post in posts
        li
          a(href=post.url)= post.title
    a.btn.btn-secondary(href='/posts/categorias') Volver
  else
    p ¿Seguro que deseas eliminar la categoría "#{categoria.name}"?
    form(method='POST' action='')
      button.btn.btn-danger(type='submit') Sí, eliminar
      a.btn.btn-secondary(href='/posts/categorias') Cancelar
